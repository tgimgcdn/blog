---
title: 参界说
description: 欢迎来到我的博客
template: splash
hero:
  tagline: 分享技术，记录生活!
head:
  - tag: script
    attrs:
      type: text/javascript
    content: |
      document.addEventListener('DOMContentLoaded', function() {
        // 使用正确的选择器
        const titleElement = document.querySelector('h1[data-page-title]');
        const taglineElement = document.querySelector('.tagline');
        
        // 设置标题样式
        if (titleElement) {
          titleElement.style.textAlign = 'center';
          titleElement.style.width = '100%';
          titleElement.style.display = 'block';
          titleElement.style.margin = '0 auto';
        }
        
        // 设置标签样式
        if (taglineElement) {
          taglineElement.style.textAlign = 'center';
          taglineElement.style.width = '100%';
          taglineElement.style.display = 'block';
          taglineElement.style.margin = '1rem auto 0';
        }
        
        // 如果父元素存在，也设置其样式
        const parentElement = titleElement?.parentElement;
        if (parentElement) {
          parentElement.style.display = 'flex';
          parentElement.style.flexDirection = 'column';
          parentElement.style.alignItems = 'center';
          parentElement.style.justifyContent = 'center';
          parentElement.style.width = '100%';
          parentElement.style.textAlign = 'center';
        }
      });
---

import HomePage from '../../components/HomePage.astro';

<HomePage pageNum={1} />
